import { CommentListDirective } from './comment-list.directive';

describe('CommentListDirective', () => {
  it('should create an instance', () => {
    const directive = new CommentListDirective();
    expect(directive).toBeTruthy();
  });
});
